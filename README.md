# LENSIQ Recovered Kotlin

Best-effort Android/Kotlin reconstruction from the provided architecture report and chat context.

Open in Android Studio, sync Gradle, then replace placeholder service/device/AI/media/translation extension points with the corresponding deobfuscated logic.

This project intentionally contains only minimal placeholder XML values. Real drawable/layout resources are not included.
